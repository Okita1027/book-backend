<Window x:Class="book_frontend.Views.Windows.BookEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:converters="clr-namespace:book_frontend.Converters"
        xmlns:viewModels="clr-namespace:book_frontend.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Height="600" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Style="{StaticResource MaterialDesignWindow}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        d:DataContext="{d:DesignInstance viewModels:BookEditViewModel }">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../ResourceDictionaries/BookEditDialogStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
            <converters:EditModeToSaveTextConverter x:Key="EditModeToSaveTextConverter" />
        </ResourceDictionary>
    </Window.Resources>

    <Grid Style="{StaticResource BookEditDialogContainerStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 加载遮罩 -->
        <Grid Grid.Row="0" Grid.RowSpan="3"
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
              Style="{StaticResource LoadingMaskContainerStyle}">
            <StackPanel Style="{StaticResource LoadingIndicatorStyle}">
                <ProgressBar Style="{StaticResource LoadingProgressBarStyle}" />
                <TextBlock Text="加载中..." Style="{StaticResource LoadingTextStyle}" />
            </StackPanel>
        </Grid>

        <!-- 主要内容区域 -->
        <ScrollViewer Grid.Row="0" Style="{StaticResource FormScrollViewerStyle}"
                      IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
            <StackPanel>

                <!-- 图书标题 -->
                <StackPanel Style="{StaticResource FormFieldContainerStyle}">
                    <TextBlock Text="图书标题" FontWeight="Bold" />
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FormTextBoxStyle}"
                             MaxLength="200"
                             materialDesign:HintAssist.Hint="请输入图书标题" />
                </StackPanel>

                <!-- ISBN 和 出版日期 -->
                <Grid Style="{StaticResource QuantityInputGridStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Style="{StaticResource FormFieldContainerStyle}">
                        <TextBlock Text="ISBN" FontWeight="Bold" />
                        <TextBox Text="{Binding Isbn, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource FormTextBoxStyle}"
                                 MaxLength="13"
                                 PreviewTextInput="IsbnTextBox_PreviewTextInput"
                                 materialDesign:HintAssist.Hint="请输入ISBN" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Style="{StaticResource FormFieldContainerStyle}">
                        <TextBlock Text="出版日期" FontWeight="Bold" />
                        <DatePicker SelectedDate="{Binding PublishedDate}"
                                    Style="{StaticResource FormDatePickerStyle}" />
                    </StackPanel>
                </Grid>

                <!-- 库存和可用数量 -->
                <Grid Style="{StaticResource QuantityInputGridStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Style="{StaticResource FormFieldContainerStyle}">
                        <TextBlock Text="库存数量" FontWeight="Bold" />
                        <TextBox Text="{Binding Stock, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource FormTextBoxStyle}"
                                 PreviewTextInput="NumberTextBox_PreviewTextInput" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Style="{StaticResource FormFieldContainerStyle}">
                        <TextBlock Text="可用数量" FontWeight="Bold" />
                        <TextBox Text="{Binding Available, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource FormTextBoxStyle}"
                                 PreviewTextInput="NumberTextBox_PreviewTextInput" />
                    </StackPanel>
                </Grid>

                <!-- 作者选择 和 出版社选择 -->
                <Grid Style="{StaticResource QuantityInputGridStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Style="{StaticResource FormFieldContainerStyle}">
                        <TextBlock Text="作者" FontWeight="Bold" />
                        <ComboBox materialDesign:HintAssist.Hint="选择作者"
                                  materialDesign:HintAssist.IsFloating="True"
                                  ItemsSource="{Binding Authors}"
                                  SelectedValue="{Binding SelectedAuthorId}"
                                  SelectedValuePath="Id"
                                  DisplayMemberPath="Name"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Style="{StaticResource FormFieldContainerStyle}">
                        <TextBlock Text="出版社" FontWeight="Bold" />
                        <ComboBox materialDesign:HintAssist.Hint="选择出版社"
                                  materialDesign:HintAssist.IsFloating="True"
                                  ItemsSource="{Binding Publishers}"
                                  SelectedValue="{Binding SelectedPublisherId}"
                                  SelectedValuePath="Id"
                                  DisplayMemberPath="Name"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}" />
                    </StackPanel>
                </Grid>

                <!-- 分类选择 -->
                <GroupBox Header="分类选择" Style="{StaticResource CategorySelectionGroupBoxStyle}">
                    <ScrollViewer Style="{StaticResource CategorySelectionScrollViewerStyle}">
                        <ItemsControl ItemsSource="{Binding Categories}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Name}"
                                              IsChecked="{Binding IsSelected}"
                                              Style="{StaticResource CategoryCheckBoxStyle}"
                                              Margin="0,2" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </GroupBox>

            </StackPanel>
        </ScrollViewer>

        <!-- 验证错误信息 -->
        <Border Grid.Row="1"
                Style="{StaticResource ValidationErrorContainerStyle}"
                Visibility="{Binding HasValidationErrors, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="{Binding ValidationErrorMessage}" Style="{StaticResource ValidationErrorTextStyle}" />
        </Border>

        <!-- 按钮区域 -->
        <Border Grid.Row="2" Style="{StaticResource ButtonAreaContainerStyle}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                <Button Content="取消"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource CancelButtonStyle}"
                        IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBooleanConverter}}" />

                <Button Content="{Binding IsEditMode, Converter={StaticResource EditModeToSaveTextConverter}}"
                        Command="{Binding SaveCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource SaveButtonStyle}">
                            <Setter Property="IsEnabled" Value="True" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSaving}" Value="True">
                                    <Setter Property="IsEnabled" Value="False" />
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <StackPanel Orientation="Horizontal">
                                                <ProgressBar Style="{StaticResource SavingProgressBarStyle}" />
                                                <TextBlock Text="保存中" Style="{StaticResource SavingTextStyle}" />
                                            </StackPanel>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>