<Page x:Class="book_frontend.Views.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:h="clr-namespace:book_frontend.Helpers"
      xmlns:views="clr-namespace:book_frontend.Views.UserControls"
      xmlns:viewModels="clr-namespace:book_frontend.ViewModels"
      mc:Ignorable="d"
      d:DesignHeight="480" d:DesignWidth="800"
      d:DataContext="{d:DesignInstance Type=viewModels:LoginViewModel, IsDesignTimeCreatable=False}">

    <!-- 外层网格用于居中内容，并准备一个遮罩层 -->
    <Grid>
        <!-- 内容区：登录表单 -->
        <Grid Margin="24" HorizontalAlignment="Center" VerticalAlignment="Center" Width="420">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="12" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="12" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="12" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 标题 -->
            <TextBlock Text="登录到图书阅览系统"
                       FontSize="20"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8" />

            <!-- 间隔 -->
            <Border Grid.Row="1" />

            <!-- 邮箱地址 -->
            <TextBox Grid.Row="2"
                     materialDesign:HintAssist.Hint="邮箱地址"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,8" />

            <!-- 间隔 -->
            <Border Grid.Row="3" />

            <!-- 密码（使用附加属性绑定） -->
            <PasswordBox Grid.Row="4"
                         materialDesign:HintAssist.Hint="密码"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         h:PasswordBoxHelper.BindPassword="True"
                         h:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,8" />

            <!-- 间隔 -->
            <Border Grid.Row="5" />

            <!-- 错误信息（非空时显示为红色文本） -->
            <TextBlock Grid.Row="6"
                       Text="{Binding ErrorMessage}"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Invert}"
                       TextWrapping="Wrap" />

            <!-- 间隔 -->
            <Border Grid.Row="7" />

            <!-- 登录按钮 -->
            <Button Grid.Row="8"
                    Content="登录"
                    Command="{Binding LoginCommand}"
                    Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                    HorizontalAlignment="Stretch"
                    Height="36" />
        </Grid>

        <!-- 遮罩层：加载时显示 -->
        <views:LoadingMask IsBusy="{Binding IsLoading}" LoadingText="正在登录..." />
    </Grid>
</Page>