<Page x:Class="book_frontend.Views.Pages.RegisterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:views="clr-namespace:book_frontend.Views.UserControls"
      xmlns:viewModels="clr-namespace:book_frontend.ViewModels"
      xmlns:constants="clr-namespace:book_frontend.Constants"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance viewModels:RegisterViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 主要内容区域 -->
        <Border Grid.Column="1" Style="{StaticResource PageMainContainerStyle}">
            <!-- 使用注册表单用户控件 -->
            <views:RegisterFormView DataContext="{Binding}">
                <views:RegisterFormView.FooterContent>
                    <StackPanel Style="{StaticResource FooterActionAreaStyle}">
                        <TextBlock Text="已有账户？" Style="{StaticResource LinkTextStyle}" />
                        <Button Content="{x:Static constants:UiConstant.GoToLoginButton}"
                                Style="{StaticResource LinkButtonStyle}"
                                Click="LoginButton_Click" />
                    </StackPanel>
                </views:RegisterFormView.FooterContent>
            </views:RegisterFormView>
        </Border>

        <!-- 遮罩层（加载时显示） -->
        <views:LoadingMask Grid.Column="1" IsBusy="{Binding IsLoading}" LoadingText="{x:Static constants:UiConstant.RegisterLoading}" />
    </Grid>
</Page>