<UserControl x:Class="book_frontend.Views.UserControls.RegisterFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewModels="clr-namespace:book_frontend.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:RegisterViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="用户注册"
                   FontSize="28" FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,10,0,5"
                   Foreground="#2196F3" />

        <!-- 注册表单主体 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="40,0,40,0">
            <StackPanel VerticalAlignment="Top">
                <materialDesign:PackIcon Kind="Account" Foreground="#2196F3" Width="24" Height="24" HorizontalAlignment="Left" Margin="0,0,0,5" />
                <TextBox x:Name="NameTextBox"
                         materialDesign:HintAssist.Hint="请输入姓名"
                         materialDesign:HintAssist.IsFloating="True"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,20"
                         FontSize="16" />

                <materialDesign:PackIcon Kind="Email"
                                         Foreground="#2196F3"
                                         Width="24" Height="24"
                                         HorizontalAlignment="Left"
                                         Margin="0,0,0,5" />
                <TextBox x:Name="EmailTextBox"
                         materialDesign:HintAssist.Hint="请输入邮箱地址"
                         materialDesign:HintAssist.IsFloating="True"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,20"
                         FontSize="16" />

                <materialDesign:PackIcon Kind="Lock" Foreground="#2196F3" Width="24" Height="24" HorizontalAlignment="Left" Margin="0,0,0,5" />
                <PasswordBox x:Name="PasswordBox"
                             materialDesign:HintAssist.Hint="请输入密码"
                             materialDesign:HintAssist.IsFloating="True"
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                             materialDesign:PasswordBoxAssist.Password="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,20"
                             FontSize="16" />

                <materialDesign:PackIcon Kind="LockCheck" Foreground="#2196F3" Width="24" Height="24" HorizontalAlignment="Left" Margin="0,0,0,5" />
                <PasswordBox x:Name="ConfirmPasswordBox"
                             materialDesign:HintAssist.Hint="请确认密码"
                             materialDesign:HintAssist.IsFloating="True"
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                             materialDesign:PasswordBoxAssist.Password="{Binding ConfirmPassword, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,30"
                             FontSize="16" />

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           FontSize="14"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />

                <TextBlock Text="{Binding SuccessMessage}"
                           Foreground="Green"
                           FontSize="14"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"
                           Visibility="{Binding SuccessMessage, Converter={StaticResource StringToVisibilityConverter}}" />

                <Button Content="注册"
                        Command="{Binding RegisterCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="#2196F3"
                        Foreground="White"
                        FontSize="16"
                        Height="45"
                        Margin="0,10,0,0"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" />
            </StackPanel>
        </ScrollViewer>

        <!-- 底部内容容器（交由宿主页面决定点击行为） -->
        <ContentPresenter Grid.Row="2" Content="{Binding FooterContent, RelativeSource={RelativeSource AncestorType=UserControl}}" />
    </Grid>
</UserControl>