<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 搜索表单容器样式 -->
    <Style x:Key="SearchFormContainerStyle" TargetType="Border">
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="Beige"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Margin" Value="10"/>
    </Style>

    <!-- 表单字段容器样式 -->
    <Style x:Key="FormFieldContainerStyle" TargetType="StackPanel">
        <Setter Property="Margin" Value="6"/>
        <Setter Property="Orientation" Value="Vertical"/>
    </Style>

    <!-- 表单标签样式 -->
    <Style x:Key="FormLabelStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Margin" Value="0,0,0,4"/>
        <Setter Property="Foreground" Value="#333"/>
    </Style>

    <!-- 表单输入框样式 -->
    <Style x:Key="FormTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignTextBox}">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Margin" Value="0,8,0,0"/>
    </Style>

    <!-- 日期选择器样式 -->
    <Style x:Key="FormDatePickerStyle" TargetType="DatePicker" BasedOn="{StaticResource MaterialDesignDatePicker}">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Margin" Value="0,8,0,0"/>
    </Style>

    <!-- 表单按钮组容器样式 -->
    <Style x:Key="FormButtonGroupStyle" TargetType="StackPanel">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="Margin" Value="6"/>
    </Style>

    <!-- 表单重置按钮样式 -->
    <Style x:Key="FormResetButtonStyle" TargetType="Button">
        <Setter Property="Margin" Value="0,0,8,0"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="Background" Value="#F5F5F5"/>
        <Setter Property="BorderBrush" Value="#DDD"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="#666"/>
        <Setter Property="FontSize" Value="14"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#E0E0E0"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- 表单搜索按钮样式 -->
    <Style x:Key="FormSearchButtonStyle" TargetType="Button">
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="Background" Value="#2196F3"/>
        <Setter Property="BorderBrush" Value="#2196F3"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="14"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#1976D2"/>
                <Setter Property="BorderBrush" Value="#1976D2"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- 图标样式 -->
    <Style x:Key="FormIconStyle" TargetType="materialDesign:PackIcon" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
        <Setter Property="Foreground" Value="#2196F3"/>
        <Setter Property="Width" Value="24"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Margin" Value="0,0,0,5"/>
    </Style>

</ResourceDictionary>